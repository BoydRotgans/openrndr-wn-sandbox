// sketch-variant-of: CourseStack
/**
 * v2, the panels: `WAND_27`, the catalogue's wall with a doorway, standing face to face in a line,
 * in line drawing only. The stack engine of [stackCourse] with the panel layout; `ROW_*` in `.env`
 * steers it the way `STACK_*` steers the beams.
 *
 *     ./gradlew run -Popenrndr.application=CourseRowKt
 */
fun main() = rowMain()
